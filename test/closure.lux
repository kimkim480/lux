constellation main

fn Prism() {
  let lamb: Function<Light> = closure();
  
  lamb();
  emit lamb();
}

fn closure() -> Function<Light> {
  let i: Light = 0;

  fn inc() -> Light {
    return i += 1;
  }

  return inc;
}